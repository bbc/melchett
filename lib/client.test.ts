import nock from 'nock';
import { HttpClient } from './client';

describe('client', () => {
    const client = new HttpClient({ name: 'foo' })
    
    describe('when "get" is called', () => {
        it('should return a response', () => {
            
        })
    })
});